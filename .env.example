# Copy to .env (gitignored) and fill in real values.
# bench/harvest.py loads .env at startup via python-dotenv.

OPENAI_API_KEY=sk-proj-REPLACE_ME
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME

# Optional overrides (defaults come from harvest-config.yaml):
# RECALL_SPEND_CAP_USD=5.0
# RECALL_HARVEST_LIMIT=10
